name: Publish Dummy

on:
  pull_request:
    types:
      - synchronize
      - opened

permissions:
  contents: read

jobs:
  publish-dummy:
    name: Publish Dummy
    uses: ApexStudios-Dev/.github/.github/workflows/_publish-dummy.yml@master
    secrets: inherit
    with:
      java-version: 25
      java-vendor: temurin
      artifact-id: itemresistance
      display-name: ItemResistance
      use-self-runner: false